class Solution(object):
    def duplicateZeros(self, arr):
        result=[]
        for i in arr:
            result.append(i)
            if i==0:
                result.append(0)
        for i in range(len(arr)):
                 arr[i]=result[i]
